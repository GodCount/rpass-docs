# WebDAV 同步

使用 WebDAV 在设备间同步密码库。

## 配置WebDAV

- **位置：**设置＞备份＞同步设置

<img height="784" alt="webdav_sync_login" src="/img/webdav_sync_login.png" />

1. 从密码库中选取已有账号。
2. 接口地址。
3. 账号/邮箱。
4. 应用密码。
    > **注意**：这个不是您的服务登录密码，而是第三方服务使用的应用密码，具体请查看对应服务的文档。

如果没有绑定到密码库，登录成功后会提示您是否存储到密码库，存储后再别的设备上导入将自动绑定上。

